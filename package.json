{
  "name": "bananachat",
  "version": "1.0.0",
  "description": "Firebase tabanlı sohbet uygulaması",
  "main": "index.html",
  "scripts": {
    "dev": "live-server --port=3000",
    "build": "mkdir -p public 2>/dev/null || mkdir public && cp *.html *.js *.css public/ 2>/dev/null || (copy *.html public\\ & copy *.js public\\ & copy *.css public\\) && echo 'Build completed'"
  },
  "dependencies": {
    "firebase": "^10.7.1"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}